# Agent Instructions

- Use `uv` for this project.
- Install and sync dependencies with `uv sync`.
- Run Python commands through `uv run`.
- Keep dependency changes in `pyproject.toml` and update `uv.lock`.
