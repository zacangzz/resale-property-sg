[project]
name = "resale-property-sg"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "ipykernel",
    "pandas",
    "pyarrow",
    "python-dotenv",
    "requests",
    "seaborn",
]
